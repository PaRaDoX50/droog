enum LoggedInStatus{
  notLoggedIn,
  halfProfileLeft,
  loggedIn,

}
enum PictureFor{
  profilePicture,
  postPicture
}

enum FollowStatus{
  requestSent,
  following,
  requestNotSent,
}

enum PostIs{
  normalPost,
  response,
  replyToAResponse,
}

enum VoteStatus{
  alreadyVoted,
  notVoted,
}

enum VoteType{
  upVote,
  undoUpVote,

}